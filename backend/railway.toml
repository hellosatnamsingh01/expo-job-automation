[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# startCommand is intentionally omitted — each service sets its own via Railway UI
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
